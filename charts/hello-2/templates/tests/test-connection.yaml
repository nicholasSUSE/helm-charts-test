apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Release.Name }}-connection-test"
  labels:
    app: {{ .Release.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
spec:
  template:
    spec:
      containers:
      - name: test
        image: busybox
        command: ['sh', '-c', 'output=$(wget -qO- http://{{ .Release.Name }}--service:{{ .Values.service.port }}/hello-2); echo "$output"']
      restartPolicy: Never
